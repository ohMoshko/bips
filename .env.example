# ===========================================
# Bips SDK - Environment Variables
# ===========================================
# Copy this file to .env.local and fill in your values
# NEVER commit .env.local to version control
# ===========================================

# ------------------------------------------
# Dynamic SDK Configuration
# ------------------------------------------
# Get your environment ID from https://app.dynamic.xyz
# Dashboard > Developer > API > Environment ID
VITE_DYNAMIC_ENVIRONMENT_ID=your-dynamic-environment-id-here

# ------------------------------------------
# Bips Payment Configuration
# ------------------------------------------
# The wallet address that will receive payments
# This should be YOUR wallet address on the target network
VITE_BIPS_RECEIVER_ADDRESS=0xYourWalletAddressHere

# Network to use for payments
# Options: base-sepolia (testnet), base (mainnet)
VITE_BIPS_NETWORK=base-sepolia

# ------------------------------------------
# Server Configuration (for apps/mathler/server)
# ------------------------------------------
# Port for the backend server
PORT=3001

# Same receiver address for server-side validation
BIPS_RECEIVER_ADDRESS=0xYourWalletAddressHere

# Network for server-side
BIPS_NETWORK=base-sepolia
